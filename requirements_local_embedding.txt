# Requirements для локального embedding сервера
# Запускается НА ХОСТЕ для максимальной производительности на Apple Silicon

# Core ML dependencies
torch>=2.0.0
sentence-transformers>=2.2.2
transformers>=4.30.0
tokenizers>=0.13.0

# FastAPI и веб-сервер
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Научные вычисления
numpy>=1.24.0

# HTTP клиент для тестирования
httpx>=0.24.0
requests>=2.31.0

# Логирование и мониторинг
structlog>=23.0.0

# Утилиты
python-dotenv>=1.0.0

# Для Apple Silicon оптимизации
# Эти пакеты автоматически используют Metal Performance Shaders
accelerate>=0.20.0

# Development dependencies (опционально)
pytest>=7.4.0
pytest-asyncio>=0.21.0
